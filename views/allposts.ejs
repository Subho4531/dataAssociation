<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen max-h-max bg-zinc-900 p-10">
        <div class="posts mt-10">

            <h1 class="text-zinc-500 text-xl mt-2">All Posts : </h1>
            <div class="post-container flex justify-items-start flex-wrap gap-2">
                <% posts.forEach((post) => {%>
                    
                    <div class="post  text-white w-72 border-zinc-600 border-2 rounded-lg bg-zinc-800 p-3 my-2 text-sm text-pretty tracking-tight">
                        <a class="text-blue-500 py-2">@<%=post.user.username%></a>
                        <p class=""><%=post.content %></p>
                        <small class="mt-2"><%=post.likes.length%> likes</small>
                        <div class="btns mt-0.5 px-1 flex justify-between">
                            <a href="/like/<%=post._id %>">
                                <%=post.likes.indexOf(post.user._id) === -1 ? "❤️" : "💓" %>
                            </a>
                            <div class="flex gap-2">
                                <% if(post.user._id == user.id){ %>

                                <a href="/edit/<%=post._id %>" class="text-amber-300 ">📝</a>
                                <a href="/delete/<%=post._id %>" class="text-amber-300">❌</a>

                                <% } %>
                            </div>
                            
                        </div>
                    </div>
                    <% }); %>
            
        </div>
            
        </div>
        
    </div>
</body>
</html>